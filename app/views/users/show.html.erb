<p style="color: green"><%= notice %></p>

<h1 class= "general-heading">
  Welcome, <%= @user.name %> 👾! Make your Post Now🍻
</h1>

<%= render @user.posts %>
<%= render partial: 'posts/form', locals: { user: @user } %>

<div>
  <%= link_to "Edit this user", edit_user_path(@user) %> |
  <%= link_to "Back to users", users_path %> | 

  <%= button_to "Destroy this user", @user, method: :delete, class: "button_to" %>
</div>
